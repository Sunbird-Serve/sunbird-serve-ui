// types refer to the actions that will be dispatched (called) from the components to perform respective functions 


const SET_NEEDS = "src/components/needPlans/SET_NEEDS";
const GET_NEEDS = "src/components/needPlans/GET_NEEDS";

export { SET_NEEDS, GET_NEEDS };
